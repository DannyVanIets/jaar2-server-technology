@model IEnumerable<TakenlijstManager.Models.Taak>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
Het aantal taken is @Model.Count()
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Omvang)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prioriteit)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Naam)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Omvang)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Prioriteit)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.TaakId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.TaakId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.TaakId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
Het aantal taken is @ViewBag.AlleTakenCount